export enum TicketStatus {
  OPEN = 'open',
  WAITING_REPLY = 'waiting-reply',
  REPLIED = 'replied',
  RESOLVED = 'resolved',
  IN_PROGRESS = 'in-progress',
  ON_HOLD = 'on-hold',
  NEEDS_REVIEW = 'needs-review',
}
